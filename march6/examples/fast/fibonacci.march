\ A contextual definition example, not a performance benchmark.
: zero 0 = ;
: one 1 = ;
: always drop true ;
: base dup drop ;
: step dup 1 - recur 1 1 swap 2 - recur 1 1 + ;
family fib 1 1 zero base one base always step ;
10 fib
